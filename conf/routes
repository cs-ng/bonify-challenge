GET     /                           controllers.Q3Controller.index

# Question 3
POST    /q3                         controllers.Q3Controller.q3

# Question 5
GET     /bank/:bankIdentifier       controllers.BankController.getBankByIdentifier(bankIdentifier: Long)
POST    /load                       controllers.BankController.loadData

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
